# info1-project
Operational Semantics project for the CentraleSupélec's course "Analysis of algorithms and programs" (Parcours Recherche - Info 1)

# File structure
ast_imp.py        # AST nodes + utilities
eval_imp.py       # eval_aexp / eval_bexp
sos.py            # Config, step(), run()
cfg.py            # control-flow graph
coverage.py       # bounded paths + coverage
test_imp.py       # unit tests
examples.py       # small demo programs
parser.py         # optional, later